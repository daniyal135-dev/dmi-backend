"""
ML models package. Heavy deps (torch, transformers) are NOT imported here — import submodules
directly, e.g. `from ml_models.image_detection.inference import ImageDetectionService`.
"""

__version__ = '1.0.0'
